# Harp, Tachyons And BrowserSync

You can set it via Harp with:

```console
harp init src --boilerplate seekr/tachyons-harp-browsersync
```
To run server with Browsersync:

```console
cd src
sh hsbs
```
